[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "reminder-common"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "pydantic-settings>=2.6,<3",
  "sqlalchemy[asyncio]>=2.0.36,<3",
  "asyncpg>=0.30,<1",
  "redis>=5.2,<6",
  "PyJWT>=2.9,<3",
]
